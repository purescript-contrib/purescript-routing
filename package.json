{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp build --include test --to public/test.js"
  },
  "devDependencies": {
    "pulp": "^8.1.0",
    "purescript": "^0.7.6",
    "rimraf": "^2.5.2"
  }
}
